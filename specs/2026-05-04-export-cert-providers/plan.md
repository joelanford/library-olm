# Implementation Plan

1. Add type aliases for `CertManagerCertificateProvider` and `OpenshiftServiceCACertificateProvider` in `bundle/registry/v1/registryv1.go`, importing the internal `certproviders` package
