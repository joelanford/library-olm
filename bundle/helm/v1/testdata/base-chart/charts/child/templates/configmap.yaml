apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-child
data:
  childDefaultOnly: {{ .Values.childDefaultOnly | quote }}
  parentOverride: {{ .Values.parentOverride | quote }}
  userOverride: {{ .Values.userOverride | quote }}
  global: {{ .Values.global.shared | quote }}
